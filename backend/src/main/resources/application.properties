# spring.application.name=backend
# # TEMPORARY: remove once DB set up lands and JPA entities/DataSource are configured for real.
# spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration,org.springframework.boot.hibernate.autoconfigure.HibernateJpaAutoConfiguration

spring.application.name=backend

# Import password from .env file
spring.config.import=optional:file:.env[.properties]

# Supabase Connection Pooler (IPv4 compatible)
spring.datasource.url=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.wmhrgfzpknljrvqvikfr
spring.datasource.password=${SUPABASE_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings (Notice we removed hibernate.dialect as recommended by your console log)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# GitHub REST API (GH-1.2 spike: plumbing only, no data mapping yet)
github.api.base-url=https://api.github.com
github.api.token=${GITHUB_API_TOKEN}